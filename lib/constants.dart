
class Constants {
  Constants._();

  /// used for (app folder, send to server)
  static const appName = 'BigBango';
  /// used for (app title)
  static String appTitle = 'Big Bango';
  static String appVersionName = '1.0.0';                     //match by pubspec.yaml
  static int appVersionCode = (1 * 1000) + (0 * 100) + (0);
}
