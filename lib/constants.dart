
class Constants {
  Constants._();

  /// used for (app folder, send to server)
  static const appName = 'BigBango';
  /// used for (app title)
  static String appTitle = 'Big Bango';
  static String appVersionName = '1.0.1';                     //match by pubspec.yaml
  static int appVersionCode = (1 * 100) + (0 * 10) + (1);
}
